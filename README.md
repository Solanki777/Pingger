# Pingger
PingGuard is a web application where users can register, log in, and add the URLs of their websites. The application periodically sends HTTP requests to those URLs to check if they are online and records the results. Users can monitor the health and uptime of their websites from a dashboard.
